Bootstrap: docker
From: Ubuntu:latest

%help
  Container INFO0607

%labels
  CREATOR Maxence MARCHAND

%post
  apt-get -y update
  apt-get -y install r-base
  wget https://download1.rstudio.org/desktop/bionic/amd64/rstudio-1.2.5042-amd64.deb
  dpkg -i rstudio-1.2.5042.amd64.deb
